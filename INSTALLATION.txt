╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║        🎓 STUDENT ATTENDANCE SYSTEM - INSTALLATION GUIDE         ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

📋 STEP-BY-STEP INSTALLATION INSTRUCTIONS

══════════════════════════════════════════════════════════════════
STEP 1: VERIFY PYTHON INSTALLATION
══════════════════════════════════════════════════════════════════

Open terminal/command prompt and run:

    python --version

OR

    python3 --version

Required: Python 3.8 or higher

If Python is not installed:
• Windows: Download from https://www.python.org/downloads/
• macOS: Install via Homebrew: brew install python3
• Linux: sudo apt-get install python3 python3-pip


══════════════════════════════════════════════════════════════════
STEP 2: INSTALL DEPENDENCIES
══════════════════════════════════════════════════════════════════

Navigate to the project folder in terminal:

    cd student_attendance

Install all required packages:

    pip install -r requirements.txt

OR install individually:

    pip install qrcode[pil]
    pip install opencv-python
    pip install pyzbar
    pip install pandas
    pip install Pillow
    pip install openpyxl


══════════════════════════════════════════════════════════════════
STEP 3: INSTALL SYSTEM DEPENDENCIES (FOR PYZBAR)
══════════════════════════════════════════════════════════════════

pyzbar requires additional system libraries:

WINDOWS:
    • Usually works out of the box
    • If issues occur, install Visual C++ Redistributable

MACOS:
    brew install zbar

LINUX (Ubuntu/Debian):
    sudo apt-get update
    sudo apt-get install libzbar0


══════════════════════════════════════════════════════════════════
STEP 4: VERIFY INSTALLATION
══════════════════════════════════════════════════════════════════

Test imports:

    python -c "import qrcode; import cv2; import pyzbar; print('✓ All packages installed')"

Expected output: ✓ All packages installed


══════════════════════════════════════════════════════════════════
STEP 5: PREPARE STUDENT DATA
══════════════════════════════════════════════════════════════════

Edit students.csv with your student information:

Format:
    StudentID,Name,Class
    101,John Doe,10A
    102,Jane Smith,10A
    103,Mike Johnson,10B

Rules:
• First row must be headers (StudentID,Name,Class)
• No empty rows
• All fields must be filled
• StudentID must be unique


══════════════════════════════════════════════════════════════════
STEP 6: GENERATE QR CODES
══════════════════════════════════════════════════════════════════

Run the QR code generator:

    python generate_qr.py

This will:
• Read students.csv
• Generate QR codes
• Save them in qr_codes/ folder

Expected output:
    ✓ Found 10 students
    [1/10] ✓ Generated: John Doe (101)
    ...
    ✓ Success: 10/10


══════════════════════════════════════════════════════════════════
STEP 7: PRINT QR CODES
══════════════════════════════════════════════════════════════════

1. Open the qr_codes/ folder
2. Print each student's QR code
3. Distribute to students

Tips:
• Use good quality printer
• Consider laminating for durability
• Print at appropriate size (5cm x 5cm recommended)


══════════════════════════════════════════════════════════════════
STEP 8: TEST THE SCANNER
══════════════════════════════════════════════════════════════════

Start the attendance scanner:

    python scanner.py

Controls:
• Point webcam at QR code
• Press 'q' to quit
• Press 'r' to reload attendance

Expected behavior:
• Camera window opens
• QR code is detected
• Green box appears around QR
• Attendance is marked
• Success message displayed


══════════════════════════════════════════════════════════════════
STEP 9: VIEW REPORTS
══════════════════════════════════════════════════════════════════

Generate attendance reports:

    python report_generator.py

Options:
• Daily report:      python report_generator.py
• Specific date:     python report_generator.py --date 2024-02-16
• Class-wise:        python report_generator.py --class-wise
• Export to Excel:   python report_generator.py --export excel


══════════════════════════════════════════════════════════════════
STEP 10: OPTIONAL GUI
══════════════════════════════════════════════════════════════════

Launch the graphical interface:

    python gui.py

GUI provides:
• Easy QR code generation
• Visual scanning interface
• Report viewing
• Excel export


══════════════════════════════════════════════════════════════════
🔧 TROUBLESHOOTING
══════════════════════════════════════════════════════════════════

CAMERA NOT WORKING:
• Close other apps using camera (Zoom, Skype)
• Try different camera index in code
• Check camera permissions (especially macOS)

QR CODES NOT SCANNING:
• Ensure good lighting
• Hold QR code steady 6-12 inches from camera
• Use high-quality printed QR codes
• Avoid glare/reflections

INSTALLATION ERRORS:
• Update pip: pip install --upgrade pip
• Use pip3 instead of pip on some systems
• Check Python version (must be 3.8+)

PYZBAR NOT WORKING:
• Ensure system dependencies installed (see Step 3)
• On Windows, install Visual C++ Redistributable
• On macOS: brew install zbar
• On Linux: sudo apt-get install libzbar0


══════════════════════════════════════════════════════════════════
📚 QUICK REFERENCE
══════════════════════════════════════════════════════════════════

Daily Workflow:
1. python scanner.py      → Start scanning
2. Students show QR codes → Attendance marked
3. Press 'q' to quit      → Close scanner
4. python report_generator.py → View report

Files:
• students.csv         → Student database (edit this)
• attendance.csv       → Attendance records (auto-created)
• qr_codes/           → QR code images folder
• generate_qr.py      → Generate QR codes
• scanner.py          → Scan attendance
• report_generator.py → Generate reports
• gui.py              → Graphical interface


══════════════════════════════════════════════════════════════════
✅ SYSTEM CHECK
══════════════════════════════════════════════════════════════════

Before first use, verify:

□ Python 3.8+ installed
□ All packages installed (qrcode, opencv-python, pyzbar, pandas)
□ System dependencies installed (zbar for pyzbar)
□ Camera connected and working
□ students.csv created with correct format
□ QR codes generated successfully
□ Scanner detects QR codes
□ Attendance is being logged


══════════════════════════════════════════════════════════════════
📞 SUPPORT
══════════════════════════════════════════════════════════════════

For issues:
1. Check this installation guide
2. Review error messages
3. See README.md for detailed documentation
4. Check Troubleshooting section above


══════════════════════════════════════════════════════════════════
🎉 YOU'RE READY!
══════════════════════════════════════════════════════════════════

Your Student Attendance System is now set up!

Start with:
    python generate_qr.py
    python scanner.py

Good luck! 🚀
